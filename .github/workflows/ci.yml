name: CI Pipeline

on:
  push:
    branch: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/Checkout@v4


      - name: Build Docker image
        run: docker build -t devops-app .
